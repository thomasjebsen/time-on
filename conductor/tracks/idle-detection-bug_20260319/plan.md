# Implementation Plan: Idle Detection Unreliable — Timer Resets During Active Use

**Track ID:** idle-detection-bug_20260319
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-19
**Status:** [~] In Progress

## Overview

Replace the unreliable IOKit `HIDIdleTime` idle detection with CoreGraphics `CGEventSource.secondsSinceLastEventType()`, which accurately tracks keyboard, mouse, and trackpad events on modern macOS. Also fix the idle recovery logic so sessions restart on activity, not just on system wake events.

## Phase 1: Replace Idle Detection API

Rewrite `IdleDetector.systemIdleTime()` to use CoreGraphics instead of IOKit.

### Tasks

- [x] Task 1.1: Replace IOKit import with CoreGraphics in `IdleDetector.swift`
- [x] Task 1.2: Rewrite `systemIdleTime()` to query `CGEventSource.secondsSinceLastEventType(.hidSystemState, eventType:)` across all relevant event types (keyDown, mouseMoved, leftMouseDown, rightMouseDown, scrollWheel, leftMouseDragged, rightMouseDragged)
- [x] Task 1.3: Return the minimum idle time across all queried event types (time since most recent input of any kind)

### Verification

- [x] App builds successfully with `make app`

## Phase 2: Fix Idle Recovery and Add Tests

Fix the `tick()` early-return bug that prevented idle recovery, and add automated tests.

### Tasks

- [x] Task 2.1: Fix `tick()` to check idle recovery even when `sessionStart` is nil (was returning early, preventing session restart on activity)
- [x] Task 2.2: Make `SessionManager` testable — inject `idleTimeProvider` closure, expose `tick()` and `startNewSession()` as internal
- [x] Task 2.3: Convert `main.swift` to `@main` on AppDelegate (enables `@testable import` without top-level code execution)
- [x] Task 2.4: Write automated tests covering: active use counting, below-threshold counting, idle threshold detection, idle recovery, no spurious resets, idle-return cycle
- [x] Task 2.5: Remove IOKit import from `IdleDetector.swift` (already gone — rewrite replaced it)

### Verification

- [x] All 13 test assertions pass with `make test`
- [x] App builds with `make app`

## Phase 3: Manual Verification

- [ ] Task 3.1: Run the app with `make run` and verify timer counts up during active use
- [ ] Task 3.2: Verify idle detection still works — stop all input and confirm session resets after threshold

## Final Verification

- [ ] All acceptance criteria met
- [ ] Manual test: active use keeps timer running
- [ ] Manual test: idle threshold still triggers session reset

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
