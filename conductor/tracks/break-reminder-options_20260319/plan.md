# Implementation Plan: Configurable Break Reminder Indicators

**Track ID:** break-reminder-options_20260319
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-19
**Status:** [ ] Not Started

## Overview

Implement in 4 phases: preferences/data layer, indicator implementations, color system, settings UI. Each phase is independently testable.

## Phase 1: Preferences and Data Layer

Add all new preference keys and expose before/after break state from SessionManager.

### Tasks

- [ ] Task 1.1: Add new preference properties to `Preferences.swift`: `reminderBannerEnabled`, `reminderSoundEnabled`, `reminderSoundName`, `reminderSoundVolume`, `reminderShakeEnabled`, `reminderShakeDuration`, `reminderShakeUntilClicked`, `colorBeforeBreakEnabled`, `colorBeforeBreak` (hex string), `colorAfterBreakEnabled`, `colorAfterBreak` (hex string)
- [ ] Task 1.2: Add `isOverdue` (Bool) property to `SessionManager` — true when elapsed time exceeds reminder interval. Expose via `onUpdate` callback or separate callback so `StatusBarController` can color the text.
- [ ] Task 1.3: Update `sendBreakReminder` to set `content.sound = nil` (app controls sound independently)
- [ ] Task 1.4: Write tests for `isOverdue` state transitions

### Verification

- [ ] `make test` passes
- [ ] `make app` builds

## Phase 2: Indicator Implementations

Wire up banner, sound, and shake indicators using new preferences.

### Tasks

- [ ] Task 2.1: Banner — only send `UNNotificationRequest` when `reminderBannerEnabled` is true
- [ ] Task 2.2: Sound — play selected system sound at configured volume when `reminderSoundEnabled` is true. Enumerate system sounds from `/System/Library/Sounds/`
- [ ] Task 2.3: Shake — use `reminderShakeDuration` for animation duration. Implement "shake until clicked" with `repeatCount = .greatestFiniteMagnitude`, stopped by click handler
- [ ] Task 2.4: Update `StatusBarController.handleClick` to stop continuous shake animation on click
- [ ] Task 2.5: Write tests for indicator gating (each indicator only fires when its toggle is on)

### Verification

- [ ] `make test` passes
- [ ] `make app` builds
- [ ] Manual: each indicator fires independently when enabled

## Phase 3: Color System

Implement color-coded timer text in the menu bar.

### Tasks

- [ ] Task 3.1: Update `StatusBarController.updateDisplay` to use `NSAttributedString` with foreground color based on break state and color preferences
- [ ] Task 3.2: "Before break" color — apply when `colorBeforeBreakEnabled` and session is active but not overdue
- [ ] Task 3.3: "After break" color — apply when `colorAfterBreakEnabled` and session is overdue
- [ ] Task 3.4: Handle all four combos (off/off, on/off, off/on, on/on) including default system color when both off
- [ ] Task 3.5: Write tests for color state logic

### Verification

- [ ] `make test` passes
- [ ] `make app` builds
- [ ] Manual: timer text changes color based on break state

## Phase 4: Settings UI

Reorganize preferences window with new break reminders section and all controls.

### Tasks

- [ ] Task 4.1: Restructure settings layout — Timer section (idle threshold, show seconds), then Break Reminders section, then Stay Awake, then General
- [ ] Task 4.2: Break reminders section — master toggle + interval field
- [ ] Task 4.3: Banner notification toggle (with permission flow, already built)
- [ ] Task 4.4: Sound controls — toggle, system sound dropdown, volume slider
- [ ] Task 4.5: Shake controls — toggle, duration field, "shake until clicked" checkbox
- [ ] Task 4.6: Color controls — before/after toggles with color wells (NSColorWell), default green/red
- [ ] Task 4.7: Resize window to accommodate new controls

### Verification

- [ ] `make app` builds
- [ ] Manual: all settings persist and take effect immediately

## Final Verification

- [ ] All acceptance criteria met
- [ ] All tests pass with `make test`
- [ ] Manual: each indicator works independently and in combination
- [ ] Manual: colors work as sole indicator
- [ ] Manual: "shake until clicked" stops on click
- [ ] Manual: sound volume slider works
- [ ] Settings layout is clean and intuitive

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
